<h3> 
Aquí podrás generar fácilmente links cortos para compartir en tus redes sociales.
¡Comienza ahora mismo y simplifica la forma en que compartes enlaces!
¡Descubre la magia de los links cortos con nuestra aplicación!
</h3>

<h4>Iniciar Sesion</h4>

<link rel="stylesheet" type="text/css" href="/assets/form_styles.css">
<link rel="stylesheet" type="text/css" href="/assets/application.css">

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
    <%= f.label :email, 'Correo electronico' %> <br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="field">
    <%= f.label :password, 'Contraseña' %><br />
    <%= f.password_field :password, autocomplete: "current-password" %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me, 'Recordar' %>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit "Iniciar Sesion" %>
  </div>
  
<% end %>
  
<%= render "devise/shared/links" %>

